# Crie uma função que receba uma frase e retorne a frase invertida (ex: "python" -> "nohtyp")

def inverter_string(frase):
    return frase[::-1]
entrada = input("Digite uma frase: ")
frase_invertida = inverter_string(entrada)
print(f"A frase invertida é: {frase_invertida}")
